# 巡检抓取任务启动说明

当前系统由两个 ROS 2 Humble 工作区组成：

- 底盘侧：`/home/nvidia/kybot_ws`，负责底盘、导航、车前瓶子检测、RViz 面板和 `mission_executor`。
- 机械臂侧：`/home/nvidia/Documents/elite_robot_ws`，负责机械臂、Percipio 相机、YOLO 定位和二指抓取/放置。

## 1. 所有终端统一环境

所有终端都应使用相同的 ROS Domain 和 RMW，否则底盘侧会找不到 `/yolo_grasp/*` 服务：

```bash
source /opt/ros/humble/setup.bash
export ROS_DOMAIN_ID=42
export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
```

建议先确认：

```bash
echo "$ROS_DOMAIN_ID"
echo "$RMW_IMPLEMENTATION"
```

## 两套独立完整启动指令

下面两套流程相互独立：

- **A：LinkerHand 灵巧手导航到点抓取**——按 YAML 导航到指定航点后执行 `action: grasp`，导航途中不自动检测或捡瓶。
- **B：二指导航途中自动捡瓶**——车前 D435 在导航途中检测瓶子，自动中断导航、接近、抓取、放置并继续导航。

不要同时启动两套机械臂抓取 Launch。两套程序使用相同的 `/yolo_grasp/*` 服务和 `/target_object_pose`，同时运行会互相冲突。

### A. LinkerHand 灵巧手导航到点抓取

#### A-0：配置灵巧手 CAN（每次重启或重新插拔后一次）

```bash
sudo /usr/sbin/ip link set can2 up type can bitrate 1000000
ip link show can2
```

`can2` 应显示 `UP`。

#### A-1：启动灵巧手机械臂侧

```bash
source /opt/ros/humble/setup.bash
export ROS_DOMAIN_ID=42
export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
source /home/nvidia/Documents/elite_robot_ws/install/setup.bash
source /home/nvidia/Documents/linker_hand_ros2_sdk/install/setup.bash

ros2 launch /home/nvidia/Documents/elite_robot_ws/biaoding/yolo_grasp.launch.py
```

该命令启动 Elite 机械臂、Percipio 相机、LinkerHand O6 SDK、灵巧手模式 YOLO 和抓取主程序，默认识别 `apple`。

#### A-2：启动普通底盘和导航

另开终端。灵巧手使用普通 `bringup.launch.py`，不要启动 `trash_pipeline.launch.py`：

```bash
source /opt/ros/humble/setup.bash
export ROS_DOMAIN_ID=42
export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
source /home/nvidia/kybot_ws/install/setup.bash

ros2 launch kybot_bringup bringup.launch.py
```

等待约 30 秒，让定位、Nav2 和 `mission_executor` 启动完成。

#### A-3：启动 RViz 面板

```bash
source /opt/ros/humble/setup.bash
export ROS_DOMAIN_ID=42
export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
source /home/nvidia/kybot_ws/install/setup.bash

rviz2
```

#### A-4：验证并设置识别类别

```bash
source /opt/ros/humble/setup.bash
export ROS_DOMAIN_ID=42
export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp

ros2 node list | grep mission_executor
ros2 node list | grep linker_hand
ros2 service list | grep '^/yolo_grasp/'
ros2 topic info /yolo/target_class

ros2 topic pub --once /yolo/target_class \
  std_msgs/msg/String "{data: 'apple'}"
```

#### A-5：灵巧手导航抓取 YAML

抓取发生在哪个航点，就在该点明确写 `action: grasp`：

```yaml
waypoints:
- name: apple_grasp_point
  pose: {x: 2.1, y: 0.55, z: 0.0, qx: 0.0, qy: 0.0, qz: 0.0, qw: 1.0}
  ptz_preset: 0
  capture: false
  action: grasp
- name: next_patrol_point
  pose: {x: 4.0, y: 1.1, z: 0.0, qx: 0.0, qy: 0.0, qz: 1.0, qw: 0.0}
  ptz_preset: 0
  capture: true
  action: ""
```

将 `pose` 换成实际录制的航点。抓取航点应位于目标前方的安全空间，且 yaw 正对目标。当前到点后最大直线逼近距离为 `1.5 m`，前方激光接近约 `0.7 m` 时停止。

在 RViz 面板执行：

```text
Load YAML → Start Mission
```

灵巧手流程：

```text
机械臂回 Home2
→ 导航到 action: grasp 航点
→ 底盘直线逼近
→ 调用 /yolo_grasp/grasp_hold
→ 识别并抓取 apple
→ 机械臂回 Home2
→ 灵巧手张开
→ 底盘原路退回
→ 继续后续航点
```

没有 `action: grasp` 的航点只导航/拍照。灵巧手导航途中不会自动捡瓶。

### B. 二指导航途中自动捡瓶

#### B-0：配置二指机械臂 CAN（每次重启或重新插拔后一次）

```bash
sudo /usr/sbin/ip link set can2 up type can bitrate 1000000
ip link show can2
```

#### B-1：启动二指机械臂、夹爪、Percipio 和 YOLO

```bash
source /opt/ros/humble/setup.bash
export ROS_DOMAIN_ID=42
export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
source /home/nvidia/Documents/elite_robot_ws/install/setup.bash

ros2 launch /home/nvidia/Documents/elite_robot_ws/biaoding/yolo_grasp_two_finger.launch.py \
  run_grasp_main:=false
```

#### B-2：启动二指抓取服务（headless）

等 B-1 启动约 10 秒后，另开终端：

```bash
source /opt/ros/humble/setup.bash
export ROS_DOMAIN_ID=42
export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
source /home/nvidia/Documents/elite_robot_ws/install/setup.bash

cd /home/nvidia/Documents/elite_robot_ws/biaoding
python3 yolo_grasp.py \
  --gripper two_finger \
  --target-class bottle \
  --headless
```

#### B-3：启动底盘、导航和车前 D435 自动捡瓶感知

```bash
source /opt/ros/humble/setup.bash
export ROS_DOMAIN_ID=42
export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
source /home/nvidia/kybot_ws/install/setup.bash

ros2 launch kybot_bringup trash_pipeline.launch.py \
  use_ocr:=false \
  use_rviz:=true
```

车前 D435 和 `/trash/target` 感知会延迟约 12 秒启动。

#### B-4：设置两套视觉都识别瓶子

```bash
source /opt/ros/humble/setup.bash
export ROS_DOMAIN_ID=42
export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp

# 车前 D435：导航途中找瓶子
ros2 topic pub --once /trash/target_class \
  std_msgs/msg/String "{data: 'iced tea bottle,bottle'}"

# 机械臂 Percipio：停车后精定位并抓瓶子
ros2 topic pub --once /yolo/target_class \
  std_msgs/msg/String "{data: 'bottle'}"
```

#### B-5：验证两套感知和抓取服务

```bash
ros2 topic hz /front_camera/camera/color/image_raw
ros2 topic hz /front_camera/camera/aligned_depth_to_color/image_raw
ros2 topic echo /trash/detection
```

瓶子在车前时，`/trash/detection` 应出现：

```text
"detected": true
"cls": "bottle"
"distance": 有效距离
```

再检查：

```bash
ros2 topic echo /trash/target
ros2 service list | grep '^/yolo_grasp/'
ros2 topic info /yolo/target_class
```

需要看到 `/trash/target` 的 `detected: true` 和 `distance_valid: true`，并能看到 `/yolo_grasp/grasp_hold`、`/yolo_grasp/place`、`/yolo_grasp/home2`。

#### B-6：二指自动捡瓶 YAML

二指导航途中自动捡瓶不在指定航点执行普通抓取，因此巡检点不要写 `action: grasp`：

```yaml
waypoints:
- name: patrol_1
  pose: {x: 1.2, y: 3.4, z: 0.0, qx: 0.0, qy: 0.0, qz: 0.0, qw: 1.0}
  ptz_preset: 0
  capture: false
  action: ""
- name: patrol_2
  pose: {x: 4.0, y: 1.1, z: 0.0, qx: 0.0, qy: 0.0, qz: 1.0, qw: 0.0}
  ptz_preset: 0
  capture: false
  action: ""
```

在 RViz 面板执行：

```text
Load YAML → Start Mission
```

只是启动节点不会自动捡瓶，必须有正在运行的巡检任务。自动触发还要求底盘正在导航运动，并且 `/trash/target` 连续 3 帧检测到距离不大于 `2.0 m` 的瓶子。

二指流程：

```text
导航中检测瓶子
→ 中断导航并停车确认
→ 底盘接近到约 0.55 m
→ 调用 /yolo_grasp/grasp_hold
→ 二指抓取并保持
→ 调用 /yolo_grasp/place 放置
→ 底盘原路退回
→ 继续导航当前巡检点
```

## 2. 二指机械臂侧启动

### 2.1 启动前配置 CAN

机械臂/夹爪插拔或主机重启后执行一次：

```bash
sudo /usr/sbin/ip link set can2 up type can bitrate 1000000
ip link show can2
```

`can2` 应显示 `UP`。

### 2.2 一键启动二指抓取

直接启动二指抓取：

```bash
source /opt/ros/humble/setup.bash
source /home/nvidia/Documents/elite_robot_ws/install/setup.bash

ros2 launch /home/nvidia/Documents/elite_robot_ws/biaoding/yolo_grasp_two_finger.launch.py
```

该 Launch 会启动：

- Elite 机械臂驱动；
- Percipio 彩色/深度相机；
- Inspire 4B4C 二指夹爪；
- 二指模式 YOLO 感知；
- `yolo_grasp.py --gripper two_finger --target-class bottle`。

默认目标类别已经是 `bottle`。

**当前版本注意：** Launch 虽然声明了 `grasp_headless` 参数，但内部错误地在生成 Launch 时直接比较 `LaunchConfiguration`，所以传入 `grasp_headless:=true` 目前不会把 `--headless` 加给抓取主程序，仍会打开交互终端。

确实需要无交互运行时，应让 Launch 不自动启动抓取主程序，再在另一个终端手动启动 headless 主程序：

```bash
# 终端 A：机械臂、相机、夹爪和 YOLO 感知
ros2 launch /home/nvidia/Documents/elite_robot_ws/biaoding/yolo_grasp_two_finger.launch.py \
  run_grasp_main:=false

# 终端 B：二指抓取服务，无交互模式
source /opt/ros/humble/setup.bash
source /home/nvidia/Documents/elite_robot_ws/install/setup.bash
cd /home/nvidia/Documents/elite_robot_ws/biaoding
python3 yolo_grasp.py --gripper two_finger --target-class bottle --headless
```

### 2.3 验证机械臂侧

等待启动完成后执行：

```bash
ros2 service list | grep '^/yolo_grasp/'
ros2 topic list | grep -E '/target_object_pose|/yolo/target_class'
```

应至少能看到：

```text
/yolo_grasp/grasp
/yolo_grasp/grasp_hold
/yolo_grasp/open
/yolo_grasp/close
/yolo_grasp/home
/yolo_grasp/home2
/yolo_grasp/ready
/yolo_grasp/place
/yolo_grasp/status
```

## 3. LinkerHand O6 灵巧手启动

灵巧手与二指共用机械臂、Percipio 相机、YOLO 感知和 `/yolo_grasp/*` 服务，但使用不同的机械手驱动和抓取参数。

> **灵巧手任务边界：灵巧手只执行“导航到指定航点 → 按 YAML 的 `action: grasp` 抓取”。灵巧手不执行导航途中检测瓶子、停车和自动捡瓶。** 自动捡地面瓶子的流程仅供二指使用。

### 3.1 启动前配置 CAN

当前 LinkerHand SDK 配置为右手 O6、CAN 接口 `can2`：

```bash
sudo /usr/sbin/ip link set can2 up type can bitrate 1000000
ip link show can2
```

### 3.2 一键启动灵巧手抓取

```bash
source /opt/ros/humble/setup.bash
source /home/nvidia/Documents/elite_robot_ws/install/setup.bash
source /home/nvidia/Documents/linker_hand_ros2_sdk/install/setup.bash

ros2 launch /home/nvidia/Documents/elite_robot_ws/biaoding/yolo_grasp.launch.py
```

该 Launch 会启动：

- Elite 机械臂驱动；
- Percipio 彩色/深度相机；
- LinkerHand O6 SDK；
- 灵巧手模式 YOLO 感知；
- `yolo_grasp.py`，其默认参数等价于 `--gripper linkerhand --target-class apple`。

抓取主程序会在新的终端窗口中启动，默认目标是 `apple`。需要抓其他类别时使用第 7 节的 `/yolo/target_class` 命令，或在交互终端按 `t`。

### 3.3 灵巧手无人值守启动

当前灵巧手 Launch 没有 `grasp_headless` 参数。需要仅通过 ROS 服务调度时，使用下面两个终端：

```bash
# 终端 A：机械臂、相机、LinkerHand SDK 和 YOLO 感知
source /opt/ros/humble/setup.bash
source /home/nvidia/Documents/elite_robot_ws/install/setup.bash
source /home/nvidia/Documents/linker_hand_ros2_sdk/install/setup.bash
ros2 launch /home/nvidia/Documents/elite_robot_ws/biaoding/yolo_grasp.launch.py \
  run_grasp_main:=false
```

```bash
# 终端 B：灵巧手抓取服务，无交互模式
source /opt/ros/humble/setup.bash
source /home/nvidia/Documents/elite_robot_ws/install/setup.bash
source /home/nvidia/Documents/linker_hand_ros2_sdk/install/setup.bash
cd /home/nvidia/Documents/elite_robot_ws/biaoding
python3 yolo_grasp.py --gripper linkerhand --target-class apple --headless
```

### 3.4 分终端手动启动（排错用）

```bash
# 终端 1：机械臂驱动
source /opt/ros/humble/setup.bash
source /home/nvidia/Documents/elite_robot_ws/install/setup.bash
ros2 launch my_elite_robot_cell_control start_robot.launch.py \
  headless_mode:=true launch_rviz:=false
```

```bash
# 终端 2：Percipio 相机
source /opt/ros/humble/setup.bash
source /home/nvidia/Documents/elite_robot_ws/install/setup.bash
ros2 launch percipio_camera percipio_camera.launch.py \
  color_resolution:=1280x960 depth_resolution:=1280x960
```

```bash
# 终端 3：LinkerHand O6 SDK
source /opt/ros/humble/setup.bash
source /home/nvidia/Documents/linker_hand_ros2_sdk/install/setup.bash
ros2 launch linker_hand_ros2_sdk linker_hand.launch.py
```

```bash
# 终端 4：YOLO 感知（默认 linkerhand 模式）
source /opt/ros/humble/setup.bash
source /home/nvidia/Documents/elite_robot_ws/install/setup.bash
cd /home/nvidia/Documents/elite_robot_ws/YOLO
python3 yolo_grasp_perception.py --target-class apple --mode linkerhand
```

```bash
# 终端 5：灵巧手抓取主程序
source /opt/ros/humble/setup.bash
source /home/nvidia/Documents/elite_robot_ws/install/setup.bash
source /home/nvidia/Documents/linker_hand_ros2_sdk/install/setup.bash
cd /home/nvidia/Documents/elite_robot_ws/biaoding
python3 yolo_grasp.py --gripper linkerhand --target-class apple
```

交互按键：`g` 抓取、`o` 张开、`c` 闭合、`p` 打印目标、`f` 打印力、`h` 回零、`2` 回 Home2、`r` 到预备位姿、`j` 示教放置位姿、`l` 放置、`e` 开关持续识别、`t` 切换类别、`q` 退出。

### 3.5 验证和手动测试

```bash
ros2 node list | grep linker_hand_sdk
ros2 topic info /cb_right_hand_control_cmd
ros2 topic info /cb_right_hand_setting_cmd
ros2 service list | grep '^/yolo_grasp/'
```

单次抓取，成功归位后自动张手：

```bash
ros2 service call /yolo_grasp/grasp std_srvs/srv/Trigger
```

导航调度使用 `/grasp_hold`；当前灵巧手专用行为是抓取成功后回 Home2 并自动张手：

```bash
ros2 service call /yolo_grasp/grasp_hold std_srvs/srv/Trigger
```

灵巧手导航抓取不执行放置动作，也不需要 `/place` 或 `place_pose.json`。`/grasp_hold` 这个服务名由调度器共用，但灵巧手分支不会保持到放置点：它会完成“抓取 → Home2 → 张手”后再返回服务结果。二指分支仍然保持夹持并等待 `/place`。

> **不要同时启动灵巧手版和二指版。** 两个版本都会创建同名的 `/yolo_grasp/*` 服务、`/target_object_pose` 和 YOLO 感知节点；同时运行会导致请求落到错误进程或目标坐标混用。切换夹爪时先完整关闭另一个版本。

### 3.6 灵巧手“导航到点后抓取”任务

灵巧手机械臂侧按第 3.2 节启动。底盘侧只启动普通 `bringup.launch.py`，**不要启动 `trash_pipeline.launch.py`**：

```bash
source /opt/ros/humble/setup.bash
source /home/nvidia/kybot_ws/install/setup.bash
ros2 launch kybot_bringup bringup.launch.py
```

如需 RViz 面板，另开终端：

```bash
source /opt/ros/humble/setup.bash
source /home/nvidia/kybot_ws/install/setup.bash
rviz2
```

灵巧手任务通过 YAML 明确指定在哪个航点抓取，例如：

```yaml
waypoints:
- name: apple_grasp_point
  pose: {x: 2.1, y: 0.55, z: 0.0, qx: 0.0, qy: 0.0, qz: 0.0, qw: 1.0}
  ptz_preset: 0
  capture: false
  action: grasp
- name: next_patrol_point
  pose: {x: 4.0, y: 1.1, z: 0.0, qx: 0.0, qy: 0.0, qz: 1.0, qw: 0.0}
  ptz_preset: 0
  capture: true
  action: ""
```

执行逻辑是：

```text
导航到 apple_grasp_point
→ 执行该点的 action: grasp
→ mission_executor 直线逼近
→ 调用 /yolo_grasp/grasp_hold
→ 灵巧手抓取、回 Home2 并张手
→ 原路退回
→ 继续导航后续航点
```

没有写 `action: grasp` 的航点只执行普通导航/拍照。因为普通 `bringup.launch.py` 不启动 `/trash/target` 的车前瓶子感知，所以灵巧手导航途中不会因检测到瓶子而中断或自动抓取。

## 4. 二指自动捡瓶：底盘、导航和车前感知启动

本节和第 5 节是**二指地面自动捡瓶专用流程**，不用于灵巧手导航抓取。

另开一个终端：

```bash
source /opt/ros/humble/setup.bash
source /home/nvidia/kybot_ws/install/setup.bash

ros2 launch kybot_bringup trash_pipeline.launch.py
```

该 Launch 会启动底盘、定位、Nav2、`mission_executor`、D435 车前瓶子检测和 RViz 面板。

当前 IMU 端口已经配置为：

```text
/dev/ttyCH341USB0，921600 baud
```

验证：

```bash
ros2 node list | grep mission_executor
ros2 topic echo /trash/target
ros2 topic echo /mission/status
```

## 5. 二指巡检途中自动捡瓶流程

启动 `trash_pipeline.launch.py` 后，`mission_executor` 在任务导航期间监听 `/trash/target`。二指检测到瓶子的流程是：

```text
导航中检测瓶子
→ 连续 3 帧满足条件且距离不大于 2.0 m
→ 中断导航并停车确认
→ 底盘接近到约 0.55 m
→ 调用 /yolo_grasp/grasp_hold
→ 抓取成功后调用 /yolo_grasp/place
→ 原路退回
→ 继续导航当前巡检点
```

因此，只想运行“巡检途中发现地面瓶子并捡起”的流程时，YAML 中不需要配置 `action: grasp`。普通巡检点使用：

```yaml
waypoints:
- pose: {x: 1.2, y: 3.4, z: 0.0, qx: 0.0, qy: 0.0, qz: 0.707, qw: 0.707}
  ptz_preset: 0
  capture: false
  action: ""
```

`action: grasp` 是“到达指定航点后再执行普通到点抓取”，会额外执行固定距离的直线逼近。地面捡瓶巡检不需要它；写上后可能在到点时再次触发普通抓取。

在 RViz 面板中加载 YAML 后点击 **Start Mission** 即可。任务开始时，`mission_executor` 会先调用 `/yolo_grasp/home2` 收拢机械臂。

## 6. 单独测试二指地面抓瓶

先启动第 2 节的二指 Launch，确保瓶子在相机视野和机械臂可达范围内，然后执行：

```bash
source /opt/ros/humble/setup.bash
source /home/nvidia/Documents/elite_robot_ws/install/setup.bash

ros2 service call /yolo_grasp/grasp std_srvs/srv/Trigger
```

`/grasp` 用于单机测试：抓取完成、机械臂归位后会自动松开夹爪。

测试“抓住并保持，等待放置”：

```bash
ros2 service call /yolo_grasp/grasp_hold std_srvs/srv/Trigger
ros2 service call /yolo_grasp/place std_srvs/srv/Trigger
```

其中 `/grasp_hold` 抓取成功后保持夹持；`/place` 到示教放置位后才松开。使用 `/place` 前必须已经在 `yolo_grasp.py` 交互终端按 `j` 保存过 `place_pose.json`。

常用手动服务：

```bash
ros2 service call /yolo_grasp/open std_srvs/srv/Trigger
ros2 service call /yolo_grasp/close std_srvs/srv/Trigger
ros2 service call /yolo_grasp/home2 std_srvs/srv/Trigger
ros2 service call /yolo_grasp/status std_srvs/srv/Trigger
```

## 7. 切换 YOLO 目标类别

当前正确的话题是 `/yolo/target_class`，不是 `/yolo_grasp/target_class`。

切换为瓶子：

```bash
ros2 topic pub --once /yolo/target_class \
  std_msgs/msg/String "data: 'bottle'"
```

切换为苹果：

```bash
ros2 topic pub --once /yolo/target_class \
  std_msgs/msg/String "data: 'apple'"
```

发送前可以检查订阅者数量：

```bash
ros2 topic info /yolo/target_class
```

如果一直显示 `Waiting for at least 1 matching subscription(s)...`，说明 YOLO 感知节点未启动、已经退出，或者两个终端的 `ROS_DOMAIN_ID`/RMW 配置不一致。

## 8. 当前二指向下抓取的力反馈

二指地面向下抓取启用了力传感器反馈，而且慢速探触阶段必须检测到接触才会闭合夹爪：

- 力数据话题：`/force_torque_sensor_broadcaster/wrench`；
- 快速下降保护阈值：实际代码为 `20 N`；
- 慢速下降：每步约 `8 mm`；
- 合力变化超过 `2.0 N`，或下降方向投影力超过 `1.2 N` 且连续命中 3 次，判定接触；
- 接触后先抬高约 `12 mm`，再闭合夹爪；
- 闭合时力超过 `2 N` 会每次抬高 `4 mm` 卸力，最多抬高 `20 mm`；
- 没有新鲜力数据时终止抓取；下降到底仍没有触力时返回“未触到物体（抓空）”，不会盲目闭合。

注意：源码中有一处旧注释仍写 `10 N`，当前实际常量是 `20 N`。

检查力传感器：

```bash
ros2 topic hz /force_torque_sensor_broadcaster/wrench
ros2 topic echo --once /force_torque_sensor_broadcaster/wrench
```

## 9. 当前拍照和深度取值方式

二指 YOLO 定位没有做多帧深度平均，当前逻辑为：

1. 深度回调持续覆盖缓存，只保留最新深度帧；
2. 彩色帧到达时使用当时缓存的最新深度帧，没有使用严格的彩色/深度时间同步器；
3. 在单张深度图的目标框中下部区域，筛出靠前的约 `15%` 深度像素；
4. 对筛出的像素取深度中位数，并计算其像素中心；
5. 二指模式直接发布该单帧计算结果，不进行跨帧位置平均；
6. 抓取程序锁存当时最新收到的目标坐标。

简而言之：**最新单帧深度 + 单帧区域鲁棒中位数**，不是多张深度图求平均。相机帧率低、彩色与深度帧错位或目标运动时，坐标可能出现跳变。

## 10. 常见错误

| 现象 | 原因与处理 |
|---|---|
| `感知节点未响应（识别未开启，放弃）` | `/vision_perception/set_enabled` 和回退服务 `/yolo_perception/set_enabled` 都不可用；检查 YOLO 感知进程和两侧 ROS 环境。 |
| `/yolo_grasp/grasp_hold NOT available` | `yolo_grasp.py` 未启动/已退出，或 ROS Domain/RMW 不一致。 |
| 目标类别命令一直等待订阅者 | 应使用 `/yolo/target_class`；同时检查感知节点是否存在。 |
| `未触到物体（抓空）` | 已完成下降但力反馈没有达到接触判据；检查目标坐标、瓶子位置和力话题。 |
| `IK 容差过大` 或姿态翻转 | 目标坐标/姿态不可达或单帧深度跳变；先检查 `/target_object_pose`，不要在地面捡瓶 YAML 中额外写 `action: grasp`。 |
| `/place` 返回“放置位姿未示教” | 在交互式 `yolo_grasp.py` 终端按 `j` 示教并保存放置关节位姿。 |
| 机械臂接受命令但不动 | 尝试 `ros2 service call /io_and_status_controller/resend_external_script std_srvs/srv/Trigger`。 |
| IMU 打不开 `/dev/ttyUSB0` | 当前应使用 `/dev/ttyCH341USB0`，并检查设备权限和实际映射。 |

## 11. 修改后重新编译

底盘工作区：

```bash
cd /home/nvidia/kybot_ws
source /opt/ros/humble/setup.bash
colcon build --symlink-install --cmake-args -DDISTRO_ROS=humble
source install/setup.bash
```

机械臂工作区：

```bash
cd /home/nvidia/Documents/elite_robot_ws
source /opt/ros/humble/setup.bash
colcon build --symlink-install --executor sequential
source install/setup.bash
```

如果 `ament_cmake_python` 报“existing path cannot be removed: Is a directory”，只清理报错包对应的 `build/<包名>` 和 `install/<包名>` 后，再重新编译该工作区；不要误编译另一个工作区。

## 12. 语音调度（AIUI 语音前端 + kybot_brain 大模型调度）

两个可选附加节点，在底盘侧运行，与上述 A/B 流程正交：面板照常可用，brain 只是另一个 `/mission/run` 客户端。

- `kybot_brain`：Qwen 大模型调度。订阅 `/brain_text`（文本指令），按 `location/location.yaml` 的点位 `name` 白名单调 `/mission/run`、`/yolo_grasp/*` 等现有服务；回复发到 `/brain_reply` 和 `/tts_text`。工作方式为**分步同步执行**：复合指令（"去点位1抓瓶子然后回来"）被拆成有序步骤，导航（`navigate_and_wait`）、雷达慢速逼近（`approach`，抓取前必用，复刻 executor driveDistance 判据：0.1m/s、±10°前向窗、0.7m 早停、上限 2m）、抓取（`grasp`，硬约束必须先逼近成功，抓完自动按逼近距离原路后退）、后退（`retreat`）、拍照（`capture_photo`）、放置（`place`，内部含张手）、张手（`arm_open`）、收臂（`arm_home`）等工具各自阻塞等真实结果，上一步失败即终止并向用户说明；纯多点跑圈可用 `run_route` 一次下发；执行中说"取消"会中断等待并调用 `/mission/cancel`。
- `robot_aiui`：已改为纯语音前端。ASR 识别文本发 `/brain_text`，订阅 `/tts_text` 播报；内置 agent/skill 已停用（代码保留，见 `robot_aiui.cpp` 的 `handleUserUtterance`）。

### 12.1 启动

```bash
# 终端 1：大模型调度
source /opt/ros/humble/setup.bash
export ROS_DOMAIN_ID=42
export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
source /home/nvidia/kybot_ws/install/setup.bash

ros2 launch kybot_brain kybot_brain.launch.py
```

```bash
# 终端 2：语音前端（不需要语音时跳过）
source /opt/ros/humble/setup.bash
export ROS_DOMAIN_ID=42
export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
source /home/nvidia/kybot_ws/install/setup.bash

ros2 launch robot_aiui robot_aiui.launch.py
```

不用语音、只发文本指令时：

```bash
ros2 topic pub --once /brain_text std_msgs/msg/String "{data: '去点位1'}"
ros2 topic echo /brain_reply
```

### 12.2 前提

- dashscope Key 写在 `src/kybot_brain/config/brain.yaml` 的 `api_key`（注意别提交进 git），或 `export DASHSCOPE_API_KEY`；需外网连通 dashscope。
- 点位白名单是 `location/location.yaml`，每个点必须有 `name`（面板暂无起名输入框，录点后手动补 name）；brain 每次调用前重读该文件，改完即生效。想说“去某点再回来”，需先录一个叫“出发点”的点位。

### 12.3 验证

```bash
ros2 node list | grep -E 'brain_node|aiui'
ros2 topic info /brain_text
tail -f ~/.kybot_brain/audit.jsonl   # 每次工具调用的审计日志
```

对麦克风说“列出所有点位”应听到点位播报；说“去点位1”应先确认 `/mission/run` 被接受、`/mission/status` 开始变化。任务进行中再发导航指令会被拒绝；说“取消任务”可随时打断。
