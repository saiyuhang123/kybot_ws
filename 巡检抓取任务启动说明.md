# 巡检 + 抓取/放置任务启动说明

多点导航巡检，在指定航点执行视觉抓取（grasp）或放置（place）。
路点、拍照、动作全部在一个 YAML 里配置。

涉及两个工作区：
- 底盘侧：`~/kybot_ws`（导航、面板、mission_executor）
- 机械臂侧：`~/Documents/elite_robot_ws`（yolo_grasp 抓取/放置）

**所有终端（两侧）必须环境一致：**

```bash
export ROS_DOMAIN_ID=42
export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
```

（不一致则 `/yolo_grasp/*` 服务互相不可见，这是最常见的坑）

---

## 一、机械臂侧启动（elite_robot_ws）

按顺序，每步确认正常再开下一个。详细说明见 `biaoding/启动说明.md` 一-b 节。

```bash
# 终端0 — CAN 口（每次插拔/重启后一次）
sudo /usr/sbin/ip link set can2 up type can bitrate 1000000

# 终端1 — 机械臂驱动
source ~/Documents/elite_robot_ws/install/setup.bash
ros2 launch my_elite_robot_cell_control start_robot.launch.py headless_mode:=true launch_rviz:=false

# 终端2 — RealSense 相机
ros2 launch realsense2_camera rs_launch.py \
  camera_namespace:=camera enable_color:=true enable_depth:=true \
  rgb_camera.color_profile:=1280x720x30 \
  depth_module.depth_profile:=640x480x30 \
  align_depth.enable:=true pointcloud.enable:=true

# 终端3 — LinkerHand 机械手 SDK
cd /home/nvidia/Documents/linker_hand_ros2_sdk
source ./install/setup.bash
ros2 launch linker_hand_ros2_sdk linker_hand.launch.py

# 终端4 — YOLO 感知
cd ~/Documents/elite_robot_ws/YOLO
python3 yolo_grasp_perception.py

# 终端5 — 抓取/放置主程序
cd ~/Documents/elite_robot_ws/biaoding
python3 yolo_grasp.py
```

验证：`ros2 service list | grep yolo_grasp` 应看到
`/yolo_grasp/grasp /open /close /home /home2 /ready /place /status`。

### 首次准备（只做一次）

1. 手眼标定 + 抓取姿态示教（按 `k`）：见 `启动说明.md`，已完成可跳过。
2. **示教放置位姿（新增）**：用 GUI/示教器把机械臂摆到放物体的位姿，
   在 `yolo_grasp.py` 终端按 `j`，关节角存入 `place_pose.json`。
   未示教就触发 place 会直接返回"放置位姿未示教"，不会乱动。

---

## 二、底盘侧启动（kybot_ws）

```bash
# 终端6 — 底盘 + 导航全系统
source ~/kybot_ws/install/setup.bash
ros2 launch kybot_bringup bringup.launch.py

# 终端7 — 任务执行器（grasp/place 动作在这里注册）
source ~/kybot_ws/install/setup.bash
ros2 launch my_rviz_panel mission_executor.launch.py

# 终端8 — RViz 面板
rviz2   # 加载含 MyPanel 的配置
```

---

## 三、配置任务 YAML

1. 面板上把车开到每个任务点，逐个 **Record Pose**；
2. **Save** 存成 `mission.yaml`；
3. 用文本编辑器批量改属性（比在 GUI 逐点 Apply 快）：

```yaml
waypoints:
- pose: {x: 1.2, y: 3.4, z: 0.0, qx: 0.0, qy: 0.0, qz: 0.707, qw: 0.707}
  ptz_preset: 3        # 0 = 不动云台
  capture: true        # 到达后拍照
  action: ""           # 无动作
- pose: {x: 2.1, y: 0.55, z: 0.0, qx: 0.0, qy: 0.0, qz: 0.0, qw: 1.0}
  ptz_preset: 0
  capture: false
  action: grasp        # 视觉抓取（抓 YOLO 当前目标）
- pose: {x: 4.0, y: 1.1, z: 0.0, qx: 0.0, qy: 0.0, qz: 1.0, qw: 0.0}
  ptz_preset: 0
  capture: false
  action: place        # 移到示教放置位姿张手，然后自动收拢回 Home2
```

`ptz_preset`/`capture`/`action` 可省略，默认 `0 / true / ""`。
可选动作还有 `home2`（收拢）和 `ready`（到预备位姿），一般不用手写：
grasp/place 流程内部已自动处理位姿切换。

**机械臂位姿规则（yolo_grasp.py 自动保证）：**

- 任务开始时 mission_executor 先调 `/yolo_grasp/home2` 收拢（服务不在则跳过）；
- grasp：先到 ready 预备位姿（相机视野最佳）→ 抓取 → **无论成败都回 Home2**；
- place：到放置位姿 → 张手 → **回 Home2**；
- 即：底盘导航期间机械臂始终处于 Home2 收拢姿态。

切换抓取目标类别：往 `/yolo/target_class` 发 String，或在 yolo_grasp 终端按 `t`。

---

## 四、跑任务

面板 **Load** 选 YAML → **Start Mission**。

每点流程：导航 → PTZ → 拍照 → 动作（grasp/place）。
动作失败（无稳定目标/IK 失败/未示教）会记录日志并中断任务；
**Cancel Mission 打断不了正在进行的抓取/放置**，要等它返回。

---

## 五、常见问题

| 现象 | 处理 |
|---|---|
| 日志 `... NOT available (yolo_grasp.py running?)` | 两侧 `ROS_DOMAIN_ID`/`RMW` 不一致，或机械臂侧进程链没起全 |
| place 返回"放置位姿未示教" | yolo_grasp 终端按 `j` 示教一次 |
| grasp 返回"无稳定目标" | 目标不在视野/在移动；先按 `p` 确认目标持续更新 |
| 机械臂接受指令但不动 | 外部控制脚本被覆盖：`ros2 service call /io_and_status_controller/resend_external_script std_srvs/srv/Trigger` |
| 旧 .txt 路点文件 | 格式已换 YAML，手动转换或重新录点 |
