# 语音调度与启动面板使用说明

本文档覆盖在原有巡检/抓取系统之上新增的三块功能：**语音大模型调度(kybot_brain)**、**KYBOT 启动面板(tools/launcher.py)**、**拍照 OCR 识别集成**。原有系统启动流程见 `巡检抓取任务启动说明.md`（其第 12 节亦有语音调度简要版）。

## 1. 总览

```
                        ┌──────────── 两条调度入口, 互为补充 ────────────┐
  语音/文本 ──> kybot_brain(Qwen 大模型) ──┐
                                            ├─> /mission/run ──> mission_executor
  KYBOT 启动面板(任务点位页) ──────────────┘        │
        │                                           ├─> /yolo_grasp/* (机械臂)
        └─ 启动管理页: 一键拉起整套系统              ├─> /hk_camera/* (相机)
                                                   └─> /ocr/recognize (OCR)
```

- **kybot_brain**：独立 ament_python 包，对现有代码零改动，删掉即还原
- **启动面板**：单文件 PyQt5 应用 `tools/launcher.py`，只做进程管理和任务编排，不改任何被管程序
- **OCR 集成**：拍照(任意路径触发)后自动识别文字，结果上 UI + 语音播报

## 2. 语音大模型调度 (kybot_brain)

### 2.1 启动

```bash
source /opt/ros/humble/setup.bash
export ROS_DOMAIN_ID=42
export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
source /home/nvidia/kybot_ws/install/setup.bash

ros2 launch kybot_brain kybot_brain.launch.py        # 调度 brain
ros2 launch robot_aiui robot_aiui.launch.py          # 语音前端(不需要语音可跳过)
```

不用语音、纯文本调试：

```bash
ros2 topic pub --once /brain_text std_msgs/msg/String "{data: '去点位1'}"
ros2 topic echo /brain_reply
```

### 2.2 配置

`src/kybot_brain/config/brain.yaml`:

| 参数 | 默认 | 说明 |
|---|---|---|
| `api_key` | (dashscope Key) | ⚠️ 勿提交 git；也可用环境变量 `DASHSCOPE_API_KEY` |
| `model` | `qwen-plus` | qwen-plus 响应 2~3s 推荐；qwen3.7-plus 推理慢(30s+)不适合分步控制 |
| `waypoints_file` | `/home/nvidia/kybot_ws/location/location.yaml` | 点位白名单, 每次调用前重读, 改完即生效 |
| `nav_timeout_sec` | 300 | 导航等终态上限 |
| `arm_timeout_sec` | 130 | 机械臂调用上限 |
| `approach_speed/stop_distance/max_distance` | 0.1 / 0.7 / 1.5 | 逼近参数, 与 executor driveDistance 一致 |

### 2.3 能力清单(工具)

| 你说 | 工具 | 行为 |
|---|---|---|
| 能去哪些点 | `list_waypoints` | 列白名单点位 |
| 去点位1 | `navigate_and_wait` | 纯导航, **等到真到达才返回** |
| 依次去点位1、点位2 / 去点位1再回来 | `run_route` | 多点一次下发(纯跑圈用) |
| 抓瓶子 | `approach` + `grasp` | **先雷达逼近(0.1m/s, 0.7m早停)再抓**, 抓完自动原路后退 |
| 放下 / 松手 | `place` / `arm_open` | 到示教位放下 / 原地张手 |
| 收臂 / 预备 | `arm_home` / `arm_ready` | Home2 收拢 / 预备位 |
| 拍张照 | `capture_photo` | 拍照 + **自动 OCR**, 识别结果一并播报 |
| 取消/停/回来 | `cancel_mission` | 执行中随时可打断 |
| 什么状态 | `get_mission_status` | 查任务进度 |

### 2.4 关键行为(和直觉一致的地方)

- **分步同步执行**: 复合指令("去点位1抓瓶子然后回来")被拆成多步, 每步等真实结果, 失败即停并说明, 不会硬往下走
- **抓取有硬门**: 没有一次成功的 approach 雷达逼近, `grasp` 直接拒绝——识别只能发生在逼近之后
- **点位白名单**: 只认 location.yaml 里的 `name`, LLM 接触不到坐标; "点位一"等中文数字自动归一化("点位一"="点位1")
- **中断有分寸**: 只有话里带"停/取消/别/算了/回"才打断执行; 查状态、闲聊一律排队
- **审计**: 每次工具调用记录在 `~/.kybot_brain/audit.jsonl`

### 2.5 语音前端 (robot_aiui)

已改造为纯语音前端： ASR 识别文本发 `/brain_text`, 订阅 `/tts_text` 播报。内置 agent/skill 已停用(代码保留在 `robot_aiui.cpp` 的 `handleUserUtterance`, 恢复方法见其注释)。注意：**急停等安全词不要走语音链路**, 保持物理手段。

## 3. KYBOT 启动面板 (tools/launcher.py)

### 3.1 启动

```bash
python3 ~/kybot_ws/tools/launcher.py
```

无需 source 任何环境(脚本自举 ROS 环境 + DOMAIN_ID=42 + cyclonedds)。双击启动：

```bash
cp ~/kybot_ws/tools/kybot-launcher.desktop ~/.local/share/applications/
# 应用菜单搜 "KYBOT启动面板"
```

配套已配置: `/etc/sudoers.d/kybot` 中 can1/can2 的 `ip link` 免密。

### 3.2 启动管理页

**机械臂模式单选(互斥)**: 二指 ⇄ 柔触手抓 ⇄ 灵巧手 ⇄ 打磨头。模式自动配对 launch：
- 二指 → `trash_pipeline.launch.py` + 二指驱动 + headless 抓取服务
- 柔触手抓 → `trash_pipeline.launch.py` + `yolo_grasp_soft_touch.launch.py` + headless 柔触抓取服务
- 灵巧手 → `bringup.launch.py` + `yolo_grasp.launch.py`
- 打磨头 → `bringup.launch.py` + 打磨状态机 + 调度桥接
- 运行中禁止切换; 切换前自动确认先停旧模式

**功能组**(各自独立启停 + 日志 tab):

| 组 | 内容 | 状态灯绿的条件 |
|---|---|---|
| 定位+Nav | bringup/trash_pipeline 全套 | `/mission/status` 在发 |
| 机械臂末端 | 按模式配对 | 抓取模式为 `/yolo_grasp/grasp_hold` 在线；打磨模式为打磨服务在线 |
| 语音调度 | kybot_brain | brain_node 在图里 |
| 语音前端 | robot_aiui | aiui 节点在图里 |
| 海康相机 | hk_camera_node | 节点在图里 |
| OCR识别 | ocr_node | `/ocr/recognize` 在线 |
| RViz | rviz2 | — |
| 建图 | IMU/雷达/转换/FAST_LIO2(错峰启动) | `/map_save` 在线 |

灯色： 灰=未启动, 橙=启动中/进程在但服务未就绪, 绿=真的能用(查 ROS 图, 不查进程)。

**自动化补链**(启动后自动做, 结果看系统日志):
- 相机(或含相机的 nav 组)启动后 → 自动 `login` + `start_stream`(不开流则 OCR 无帧)
- OCR 启动后 → 自动预热首次推理(GPU 冷启动 20~40s), 看到"OCR 预热完成"再使用

**建图互斥**: 建图运行中, 定位Nav/机械臂/语音全部禁用; 反向开启时会弹确认先停其他组。"保存"按钮调 `/map_save`。

**一键全部启动**: 配 CAN → Nav(等/mission/status)→ 机械臂(等 grasp 服务)→ 语音, 逐步带超时。"全部停止"杀所有组并清理残留。

**停止的完整性**: 主进程走 killpg 杀进程组; gnome-terminal 弹出的独立窗口(IMU/FAST_LIO/Nav2/mission_executor/抓取主程序)按命令行特征精准清扫, 不留僵尸。

### 3.3 任务点位页(第二个 tab)

**点位管理**:
- **录当前点**: 抓 map→base_link 当前位姿(需定位在跑)
- **编辑**: 名称/预置位(海康预置位号 0~256)/拍照/动作(grasp 等)。**名称供语音按名导航**("去3号仪表"), 这是 RViz 面板没有的
- **加载/保存**: 与 RViz 面板同一 YAML 格式, 默认 `/home/nvidia/kybot_ws/location/location.yaml`, **与语音调度共用**, 保存即生效

**任务控制**:
- **开始任务**: 全量点位下发 `/mission/run`; executor 不在线或有任务在跑时按钮灰
- **取消任务**: 随时可点
- **循环执行** ☑: 每轮走完(COMPLETED) 5s 后自动重发; 取消/失败/去勾自动停; 只循环本页发起的任务

**依赖服务**(与"定位+Nav"组互斥, 自动防重复): 任务调度 / 海康相机 / OCR识别 三个独立启停行, 状态实时显示。

**OCR 识别结果区**: 滚动显示两类来源(见下节)。

## 4. 拍照 OCR 集成

两条路径, 结果都进任务页的 OCR 结果区：

| 路径 | 触发 | 结果显示 |
|---|---|---|
| 传统调度(面板/语音/循环发起的巡检) | `/mission/status` 出现 CAPTURING 跳变 → 1.2s 后调 `/ocr/recognize` | `[巡检拍照] 识别到 2 处: 压力表(0.95)、23.5(0.88)` |
| 语音拍照("拍张照") | brain 拍完顺手调 OCR | 语音播报 + `[语音拍照] ...` |

前提: ① 相机流已开(启动面板会自动 login+start_stream) ② ocr_node 已启动且预热完成。OCR 未开时各路径优雅降级(只拍照), 不报错不阻塞。

## 5. 离线仿真 (tools/fake_executor.py)

不开真机也能联调 brain/面板逻辑:

```bash
export ROS_DOMAIN_ID=99   # 隔离域, 碰不到真机
source /opt/ros/humble/setup.bash && source ~/kybot_ws/install/setup.bash
python3 ~/kybot_ws/tools/fake_executor.py &
ros2 launch kybot_brain kybot_brain.launch.py    # 或 python3 tools/launcher.py
```

假服务端提供: `/mission/run`(8s 后完成)+`/mission/cancel`+`/yolo_grasp/grasp_hold`+`/hk_camera/capture`+`/ocr/recognize`+`/odom`+`/scan_fe`(前方 0.5m 恒有障碍)。注意**启动面板强制域 42**, 仿真它时用默认域即可。

## 6. 常见问题

| 现象 | 原因与处理 |
|---|---|
| OCR 报 "No frame received yet" | 相机流没开。启动面板启动相机/nav 组会自动 login+start_stream; 手动起节点需自己调 `/hk_camera/start_stream` |
| `/ocr/recognize` 调用超时 | 首次推理 GPU 冷启动(20~40s)。等"OCR 预热完成"再用; 预热后秒级 |
| 面板灯全橙不绿 | 探针异常(老版本 spin 会静默死)。升级后异常会显示在系统日志 tab 并自动恢复 |
| 点了停止, 弹出的终端窗口还在 | 老版本问题。现在按命令行特征精准清扫(IMU/FAST_LIO/Nav2/executor/抓取主程序) |
| 语音说"点位一"不识别 | 已支持中文数字归一化("点位一"="点位1", 支持十/十二/两/零) |
| 抓取在到点直接报错够不到 | 抓取前必须先雷达逼近(硬约束), 确认 approach 的雷达早停日志 |
| 抓取成功但松手了(灵巧手) | 原 yolo_grasp.py 灵巧手分支行为, 改法见 `elite_robot_ws/biaoding/yolo_grasp.py:935` 的说明(二指原生保持夹持) |
| dashscope 调用失败 | 检查外网/Key; brain 会降级回复不崩溃 |

## 7. 相关文件

```
src/kybot_brain/                 # 语音调度包 (brain_node/tools/llm_client/waypoints)
src/kybot_brain/config/brain.yaml
tools/launcher.py                # 启动面板 (单文件)
tools/kybot-launcher.desktop     # 桌面图标
tools/fake_executor.py           # 离线仿真服务端
location/location.yaml           # 点位白名单 (面板/brain 共用)
~/.kybot_brain/audit.jsonl       # 语音调度审计日志
/etc/sudoers.d/kybot             # CAN 免密
```
