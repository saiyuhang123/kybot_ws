# 巡检导航中自动抓取冰红茶瓶：实施方案

> 状态：方案评审稿
> 目标：Nav2 巡检导航过程中，车前 D435 发现冰红茶瓶 → 暂停导航 → 机械臂静态抓取 → 放置 → 恢复导航继续巡检。

## 1. 总体流程

```
RViz 面板启动巡检任务
   │
   ▼
mission_executor（现有）
   │ 1. 机械臂收拢 Home2（现有）
   │ 2. 循环路点：
   │    ├─ 导航到路点（Nav2 NavigateToPose，现有）
   │    │    └─ 途中 D435 粗检到瓶子 → 取消当前导航目标（新增）
   │    ├─ 停车确认瓶子（新增）
   │    ├─ D435 距离慢速接近（新增）
   │    ├─ 调用 /yolo_grasp/grasp 静态抓取（现有服务）
   │    ├─ 调用 /yolo_grasp/place 放置（现有服务）
   │    ├─ 原路退回一段
   │    └─ 继续导航到被中断的路点（恢复巡检）
   │ 3. PTZ / 拍照 / extra_action（现有，不受影响）
   └─ 任务完成 / 失败 / 取消
```

核心原则：

- **接近阶段只依赖车前 D435**，抓取阶段只依赖臂上图漾相机，两者不混用；
- **运动中只做粗检，停稳后才做精确确认和测距**；
- 抓取时车完全静止，不做动态抓取；
- 任何时刻只有一个节点发布 `/cmd_vel`。

## 2. 现状盘点

| 已有能力 | 位置 | 说明 |
|---|---|---|
| 路点调度 | `src/my_rviz_panel/src/mission_executor.cpp` | `/mission/run`、`/mission/cancel`、`/mission/status`，循环执行“导航→PTZ→拍照→动作” |
| 动作扩展机制 | `mission_executor.cpp` 的 `registerAction()` | 路点 `extra_action` 可触发，已有 `grasp/place/home2/ready/log` |
| 直线逼近原语 | `driveDistance()` | 基于 `/odom` 走固定距离，前激光 `/scan_fe` 小于 0.7m 提前停车，自己发 `/cmd_vel` |
| 抓取组合动作 | 已注册的 `"grasp"` | 直线逼近 → `/yolo_grasp/grasp` → 原路退回 |
| 机械臂收拢 | `executeMission()` 开头 | 任务开始自动调 `/yolo_grasp/home2` |
| 静态抓取 | `~/Documents/elite_robot_ws/biaoding/yolo_grasp.py --gripper two_finger` | 臂上图漾相机定位 + 力控抓取 + 放置 |
| 底盘速度桥接 | `yhs_can_control/cmd_vel_bridge.py` | `/cmd_vel` → 底盘速度/转向，0.5s 超时自动急停 |

结论：不需要重写 RViz 面板，也不需要重写调度器，主要改动是
`mission_executor`（C++）加一个车前感知节点（Python）。

## 3. 新增模块

### 3.1 front_perception_node（车前感知）

建议新包 `trash_mission`，放在 `~/kybot_ws/src/trash_mission/`。

订阅 D435（独立命名空间，避免与臂上相机 TF 冲突）：

- `/front_camera/camera/color/image_raw`
- `/front_camera/camera/aligned_depth_to_color/image_raw`
- `/front_camera/camera/color/camera_info`

#### 模式 A：运动粗检（导航途中）

- 只跑彩色 YOLO，不读深度；
- 每帧算 Laplacian 模糊分，低于阈值直接丢；
- 连续 3~5 帧（约 1s 窗口）检测到瓶子且框位置稳定，才发布“疑似目标”；
- 目标提示词先用 `iced tea bottle` / `bottle`，叠加琥珀色占比约束；
- 效果不好再微调 YOLO 专属模型（采集 200~500 张）。

#### 模式 B：停车确认（中断导航后）

- 车完全停稳后，用 5~10 帧确认检测框稳定；
- 深度在框内缩 20% 区域取中位数，多帧中位数差 < ±5cm 才采信；
- 发布最终距离和横向偏差。

#### 发布消息

```text
# trash_mission/msg/TrashTarget.msg
bool detected
float32 distance          # 米
float32 lateral_offset    # 米，车前方横向偏差
float32 confidence
bool   stationary_confirm # true=停车确认，false=运动中粗检
builtin_interfaces/Time stamp
```

### 3.2 mission_executor 改造

改动文件：

- `src/my_rviz_panel/include/my_rviz_panel/mission_executor.hpp`
- `src/my_rviz_panel/src/mission_executor.cpp`
- `src/hk_camera/msg/MissionStatus.msg`

#### a) 订阅瓶子话题

新增：

- `std::atomic<bool> bottle_candidate_`：运动粗检到疑似目标；
- `std::atomic<bool> bottle_confirmed_`：停车确认通过；
- 订阅 `/trash/target`。

#### b) `navigateTo()` 支持中断

现在是一次性等结果（最长 600s），改为循环等待：

- 每 100ms 轮询导航结果；
- 若 `bottle_candidate_ == true` 且处于导航状态 → `async_cancel_all_goals()`；
- 返回值从 `bool` 扩成三态：`NAV_OK / NAV_FAILED / NAV_INTERRUPTED`。

#### c) 新增 `pickupBottle()` 例程

1. 停车确认：等 `/trash/target` 的 `stationary_confirm=true`，超时 5s 视为误报；
2. 确认失败 → 直接恢复导航；
3. 确认成功 → `approachToBottle()`：
   - 复用 `driveDistance()` 框架，停车条件改为“D435 瓶子距离 ≤ d_stop”；
   - `d_stop` 实测标定，预计车前相机 0.5~0.7m；
   - 发 `/cmd_vel` 20Hz，速度分段：0.3 → 0.15 → 0.08 m/s；
   - 前激光与 `cmd_vel_bridge` 0.5s 超时急停作为双重保险；
4. 到位后调 `/yolo_grasp/grasp`（等待 120s）；
5. 调 `/yolo_grasp/place`；
6. 按实际逼近距离退回（复用 `driveDistance(..., forward=false)`）。

#### d) 恢复导航

- 抓取/放置完成（无论成败）后，重新发送被中断路点的 NavigateToPose；
- 每个路点最多被中断 N 次（默认 2），防止误检死循环；
- 误报后冷却：10s 内或车移动不足 1m 前，不再触发同一目标。

#### e) 状态上报

扩展 `MissionStatus.msg`：

```text
uint8 STATE_BOTTLE_CONFIRMING=7
uint8 STATE_APPROACHING=8
uint8 STATE_GRASPING=9
uint8 STATE_PLACING=10
uint8 STATE_RETREATING=11
```

面板的 `message` 字段继续显示中文描述，兼容现有 UI。

#### f) 保留原有行为

路点 `extra_action="grasp"` 的手动抓取模式继续保留；只有导航途中检测到
瓶子才走自动中断流程，两者互不影响。

## 4. 消息/接口总表

| 方向 | 话题/服务 | 类型 | 提供者 |
|---|---|---|---|
| 检测结果 | `/trash/target` | `trash_mission/msg/TrashTarget` | front_perception_node |
| 速度命令 | `/cmd_vel` | `geometry_msgs/Twist` | mission_executor（接近时）/ Nav2（导航时）|
| 抓取 | `/yolo_grasp/grasp` | `std_srvs/srv/Trigger` | yolo_grasp.py |
| 放置 | `/yolo_grasp/place` | `std_srvs/srv/Trigger` | yolo_grasp.py |
| 收拢 | `/yolo_grasp/home2` | `std_srvs/srv/Trigger` | yolo_grasp.py |
| 任务控制 | `/mission/run` `/mission/cancel` | 已有 | mission_executor |

## 5. 启动方式

```bash
# 终端 1：底盘 + 导航 + 调度（现有 bringup）
source /opt/ros/humble/setup.bash
source /home/nvidia/kybot_ws/install/setup.bash
ros2 launch kybot_bringup bringup.launch.py

# 终端 2：D435 + 车前感知（新增 launch）
source /home/nvidia/kybot_ws/install/setup.bash
ros2 launch trash_mission trash_mission.launch.py

# 终端 3：机械臂 + 抓取栈（现有）
source /opt/ros/humble/setup.bash
source /home/nvidia/Documents/elite_robot_ws/install/setup.bash
ros2 launch my_elite_robot_cell_control start_robot.launch.py headless_mode:=true launch_rviz:=false
ros2 run inspire_gripper Gripper_control_node
cd ~/Documents/elite_robot_ws/biaoding
python3 yolo_grasp.py --gripper two_finger
```

环境要求（两侧一致）：

```bash
export ROS_DOMAIN_ID=42
export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
```

## 6. 安全设计

- 接近时 `max_velocity` 限 0.3 m/s；`cmd_vel_bridge` 0.5s 超时自动急停；
- D435 连续 0.3s 丢目标 → 立即停车；
- 前激光 `/scan_fe` 距离 < 0.5m → 无条件停车（现有逻辑）；
- 抓取期间底盘不发速度，桥接超时后自动进空挡；
- 抓取失败不阻塞巡检：退回后继续当前/下一个路点，只记日志；
- `/mission/cancel` 任何阶段都能中断（通过 `mission_cancel_` 标志检查）。

## 7. 实施步骤与估时

| 阶段 | 内容 | 估时 |
|---|---|---|
| M1 | D435 车前固定安装、独立命名空间启动、采集样本、测试 YOLO 提示词 | 0.5~1 天 |
| M2 | `front_perception_node`：运动粗检 + 模糊过滤 + 停车确认 + 测距 | 1~1.5 天 |
| M3 | mission_executor 改造：导航中断、`approachToBottle()`、恢复导航、状态上报 | 2~3 天 |
| M4 | 端到端联调：面板跑 2~3 个路点，途中放瓶子，验证“中断→抓→续航” | 1~2 天 |
| M5 | 鲁棒性：误检冷却、重试上限、光照/瓶子姿态、抓取失败恢复 | 2~3 天 |

总计约 1.5~2 周出一个稳定版本。

## 8. 风险与对策

| 风险 | 对策 |
|---|---|
| 运动模糊导致漏检/误检 | 运动中只粗检，停车确认兜底；模糊帧过滤；巡检速度限 0.3~0.5 m/s |
| Nav2 取消目标后状态恢复慢 | 实测取消延迟，恢复导航前等 `/odom` 速度归零再重发目标 |
| 瓶子反光导致 D435 深度空洞 | ROI 内缩 + 中位数 + 多帧稳定 ±5cm 才采信 |
| 误检导致频繁停车 | 3~5 帧确认 + 冷却时间 + 每路点重试上限 |
| 两个工作区包重名（`qwen_vision` 等） | 新代码放 `kybot_ws`，抓取栈放 `elite_robot_ws`，source 顺序固定 |
| 两个相机 TF 冲突 | D435 用 `front_camera` 命名空间/前缀，臂上相机保持 `camera` |
| 抓完瓶子后车偏离导航路径 | 抓取后退回原逼近距离，再重新导航被中断路点 |

## 9. 待确认问题

1. 冰红茶瓶通常站立还是可能躺着？第一版建议只处理站立。
2. 场景室内还是室外？室外强光下 D435 深度明显变差。
3. 车前 D435 安装高度和 Nav2 巡检速度？影响粗检和 `d_stop` 标定。
4. 机械臂能否够到地面上的瓶子？若放地上，要确认 Home2→预备位路径不会撞车前相机。

## 10. 相关文件

- 现有启动说明：`~/kybot_ws/巡检抓取任务启动说明.md`
- 调度器源码：`~/kybot_ws/src/my_rviz_panel/`
- 路点/状态消息：`~/kybot_ws/src/hk_camera/msg/`
- 底盘控制：`~/kybot_ws/src/yhs_can_control/`
- 抓取主程序：`~/Documents/elite_robot_ws/biaoding/yolo_grasp.py`
