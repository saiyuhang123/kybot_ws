# 末端热切换 — 现场部署与验证清单

> 目标：让 **二指 / 柔触手抓 / 灵巧手** 三者之间可以在**不停定位导航**的前提下互换，
> 不再需要重启 nav 导致 FAST_LIO 重新收敛。打磨头不参与热换，仍需整组停启。

本文档是现场操作手册。**请按顺序执行，不要跳步** —— 第 2 步是整条链路的关键闸门，
它失败的话后面全都没意义。

---

## 0. 先看这里：本次改了什么

| 提交 | 内容 |
|---|---|
| `e3e71b3` | 解耦手抓和机械臂（末端热切换主体） |
| `d1e9969` | 修补 bug（错峰延时回退、seq 忙判断、全部停止误触发钩子） |
| `a2f8170` | 去掉柔触的路边捡瓶子功能 |

**回滚点：`d03fc1d`**（这三个提交之前的状态）

涉及文件：

```
src/my_rviz_panel/src/mission_executor.cpp          ← 需要编译
src/my_rviz_panel/include/my_rviz_panel/mission_executor.hpp  ← 需要编译
tools/launcher.py                                    ← 不需要编译，直接运行
```

### 核心原理（一句话）

`mission_executor` 的 `end_effector_mode` 从"启动时写死的参数"变成了
"运行时可改的参数"。定位、Nav2、EKF、FAST_LIO、底盘、雷达全都**与末端无关**，
所以换末端只需要改这一个参数 + 换机械臂软件栈，nav 底座全程不动。

### ⚠️ 三条最重要的行为变化

1. **`启动 定位+Nav` 现在跑的是 `bringup.launch.py`，不再是 `trash_pipeline.launch.py`**。
   车前感知和 RViz 被拆成独立组，由面板按末端自动带起（延时照搬原来的 12s / 16s）。
2. **路边捡瓶只剩二指**。柔触改成和灵巧手一样，只做到点抓取。
3. **打磨头进出仍需整停**。它用的是另一套机械臂驱动（力控 + 深度相机）。

---

## 1. 编译（只有一个包）

登录 Jetson，然后：

```bash
cd /home/nvidia/kybot_ws

# 确认代码已经是最新的三个提交
git log --oneline -3
# 期望看到: a2f8170 / d1e9969 / e3e71b3

colcon build --symlink-install --packages-select my_rviz_panel \
  --cmake-args -DDISTRO_ROS=humble

source install/setup.bash
```

**⚠️ 这段 C++ 代码从未编译过**（开发机没有 ROS 环境）。编译报错是本次现场最可能
遇到的第一个障碍。如果报错，大概率集中在这两处：

| 报错位置 | 可能原因 | 处理 |
|---|---|---|
| `OnSetParametersCallbackHandle` 找不到 | 命名空间限定问题 | 已写成完整限定 `rclcpp::node_interfaces::...`，参照 `src/hk_camera/src/hk_camera_node.cpp:52` 的同类写法对齐 |
| `RCLCPP_WARN_THROTTLE` 参数不匹配 | 宏签名差异 | 位于 `onTrashTarget` 里，实在不行直接删掉这条日志，不影响功能 |

`tools/launcher.py` **不参与 colcon 编译**，改完直接生效，重启面板即可。

---

## 2. 🔑 关键闸门：脱机验证参数机制

**这一步不碰机器人、不动机械臂**，只验证"参数能不能热改"。
如果这步不通，后面所有流程都不用试了。

### 2.1 必须先设置环境变量

```bash
source /opt/ros/humble/setup.bash
source /home/nvidia/kybot_ws/install/setup.bash
export ROS_DOMAIN_ID=42
export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
```

> **⚠️ 这两个 export 不能省。** 面板内部自己设了这两个变量
> （`launcher.py:44-51`），你从另一个终端敲 `ros2` 命令如果不设，
> **根本发现不了节点**，会误判成"节点没起来"。

### 2.2 单独起一个 mission_executor

另开一个终端（同样先做 2.1 的环境设置）：

```bash
ros2 launch my_rviz_panel mission_executor.launch.py end_effector_mode:=twofinger
```

### 2.3 逐条验证

```bash
# ① 读回初值 —— 期望: String value is: twofinger
ros2 param get /mission_executor end_effector_mode

# ② 合法热切换 —— 期望: Set parameter successful
ros2 param set /mission_executor end_effector_mode softtouch
ros2 param get /mission_executor end_effector_mode      # 期望变成 softtouch
# 节点日志应打印: End effector mode hot-switched: twofinger -> softtouch

# ③ 非法值必须被拒 —— 期望: Setting parameter failed: invalid end_effector_mode 'banana'
ros2 param set /mission_executor end_effector_mode banana
ros2 param get /mission_executor end_effector_mode      # 期望仍是 softtouch，没被改坏

# ④ 三种末端都能切
ros2 param set /mission_executor end_effector_mode linkerhand
ros2 param set /mission_executor end_effector_mode twofinger
```

### ✅ 通过标准

- ② 成功且日志有 `hot-switched` 字样
- ③ 被拒绝，且 ④ 之前的值没有被破坏

### ❌ 如果 ② 返回成功但 `param get` 值没变

说明参数回调没生效（`add_on_set_parameters_callback` 没注册上或注册位置不对）。
检查 `mission_executor.cpp` 里回调注册是否在**所有 `declare_parameter` 之后** ——
这是硬要求，因为 `declare_parameter` 自己也会触发回调。

**这一步不通就地停止，不要继续。**

---

## 3. 面板冷启动验证（机器人上电，机械臂先不动）

```bash
cd /home/nvidia/kybot_ws
python3 tools/launcher.py
```

### 3.1 界面自检

- [ ] 功能组里多了一行 **「路边捡瓶感知 (D435, 仅二指)」**
- [ ] 末端单选下方多了一行 **「实际生效末端: —(mission_executor 未运行)」**
- [ ] 未选末端时，定位Nav / 机械臂 / 语音调度 三个启动按钮是灰的（原有行为，不能坏）

### 3.2 选「二指」→ 点「启动 定位+Nav」

观察启动顺序（这是修补 `d1e9969` 的重点）：

- [ ] nav 立刻启动
- [ ] **约 12 秒后** 路边捡瓶感知才启动（不能和 nav 同时抢 CPU）
- [ ] **约 16 秒后** RViz 才启动
- [ ] 等 nav 起完，「实际生效末端」变成绿色的 **「实际生效末端: 二指」**

### 3.3 各模式启动矩阵（冷启动逐个试，或至少试二指+柔触）

| 末端 | 路边捡瓶感知 | 自动 RViz |
|---|---|---|
| 二指 | ✅ 起（延后 12s） | ✅ 起（延后 16s） |
| 柔触手抓 | ❌ 不起 | ✅ 起（延后 16s） |
| 灵巧手 | ❌ 不起 | ❌ 不起 |
| 打磨头 | ❌ 不起 | ❌ 不起 |

- [ ] 柔触模式下「路边捡瓶感知」那行的按钮应该是**灰的**（点不动）

---

## 4. 🎯 热换末端实测（核心流程）

前提：nav 已经在跑，定位已收敛，**任务处于空闲状态**。

### 4.1 先记录基准，用来事后证明"定位真的没丢"

```bash
# 记下当前位姿，热换后要对比
ros2 run tf2_ros tf2_echo map base_link
```

也在 RViz 里看一眼车的位置和朝向，截图或记下来。

### 4.2 操作顺序（严格按这个顺序）

1. **面板上点目标末端单选**（比如从「二指」点到「柔触手抓」）
   - 弹出确认框，写明"会停止机械臂软件栈和语音调度，但不会中断定位/Nav"
   - 点「Yes」

2. **等面板走完切换流程**，状态栏依次显示：
   ```
   热换末端: 停止旧末端软件栈 ...
   热换末端: 热改 mission_executor 末端参数 ...
   热换末端: 调整车前感知 ...
   热换末端: 恢复语音调度 ...
   ```
   最后出现：
   ```
   末端已热切换为「柔触手抓」，定位/Nav 未中断。
   请确认已物理更换末端后，再点「机械臂」行的启动。
   ```
   - [ ] 切换期间末端单选、机械臂、建图、一键启动按钮全部置灰
   - [ ] 「实际生效末端」最终显示绿色的「柔触手抓」

3. **✅ 立刻验证定位没丢**（这是本次改动的全部意义）
   - [ ] RViz 里车的位置**没有跳变**
   - [ ] `tf2_echo map base_link` 仍在持续输出，数值与 4.1 记录的一致
   - [ ] Nav2 仍能接受目标点（可以先不发，看 Nav2 终端窗没有报错退出即可）

4. **物理更换末端** —— 按你们现有的安全规程操作（断电/急停等）。
   面板此时是空闲状态，可以慢慢换，nav 一直在跑。

5. **换完后确认面板「实际生效末端」显示正确**，然后点「机械臂」行的**启动**

6. 等机械臂 LED 变绿（`_arm_online` 判据：柔触会额外检查
   `gripper_server` + `/gripper_command` + `/gripper_pressure`）

### 4.3 三个方向都要试一遍

- [ ] 二指 → 柔触（应停掉路边捡瓶感知）
- [ ] 柔触 → 二指（应起路边捡瓶感知，**不带 12s 延时**，因为 nav 已稳）
- [ ] 柔触 → 灵巧手（感知本来就没起，全程不动）

### 4.4 必须失败的情况（安全门验证）

- [ ] nav 在跑时，点「打磨头」→ 单选是**灰的**，点不动
- [ ] 当前是打磨头且 nav 在跑 → 四个末端单选**全灰**
- [ ] 任务正在执行中（状态不是空闲/已完成/失败/已取消）→ 末端单选**全灰**
- [ ] 建图正在运行 → 末端单选**全灰**
- [ ] 热换进行中点「全部停止」→ 流程干净中止，**不弹**"已热切换成功"的假消息，
      「实际生效末端」重新回读显示真实状态

---

## 5. 回归验证（确认没弄坏原有功能）

### 5.1 路边捡瓶范围（本次重点改动）

- [ ] **二指**：任务运行中，车前 2m 内放个瓶子 → 车应中断导航去抓
- [ ] **柔触**：同样放瓶子 → 车**不应该**停车抓瓶，并且
      `mission_executor` 日志出现（10 秒节流一次）：
      ```
      Roadside bottle pickup is twofinger-only; ignoring /trash/target
      under end effector 'softtouch'
      ```
- [ ] **柔触的到点抓取仍正常**，且前向停车用的是柔触专用 0.50m 而不是 0.70m
      （这个参数**特意保留**了，别以为是漏改）

### 5.2 原有功能

- [ ] 一键全部启动（二指）走完全程
- [ ] 打磨头：整停 → 切到打磨头 → 启动，流程与以前一致
- [ ] 建图互斥：开建图会要求先停 nav/机械臂/语音/感知
- [ ] 全部停止能停干净（`pgrep -f yolo_grasp` / `front_perception` 无残留）
- [ ] RViz 里 MyPanel 面板插件还在（nav 组换成 bringup 后，RViz 配置改由
      面板加载 `/home/nvidia/.rviz2/default.rviz`，若该文件不存在会退回裸 rviz2）

---

## 6. 故障排查

| 现象 | 原因 | 处理 |
|---|---|---|
| `ros2 param` 找不到 `/mission_executor` | 忘了 `export ROS_DOMAIN_ID=42` 和 `RMW_IMPLEMENTATION=rmw_cyclonedds_cpp` | 见 2.1 |
| 热换卡在「停止旧末端软件栈」直到超时（30s） | 旧栈节点没死干净 | 手工查：`ros2 node list \| grep -E "robot_cartesian_control\|gripper_server\|ysURForceAppControl"`，再 `pgrep -f yolo_grasp` |
| 热换卡在「热改参数」直到超时（15s） | 参数下发或回读失败 | 回到第 2 步单独验证参数机制 |
| 热换卡在「调整车前感知」（120s） | D435 USB 重枚举失败 / YOLO 模型加载慢 | `ros2 node list \| grep front_perception`；插拔 USB；模型路径 `/home/nvidia/Documents/elite_robot_ws/YOLO/yolov8x-worldv2.pt` |
| 「实际生效末端」标红不一致 | 面板与节点脱节（外部起的 nav、面板重开过） | 重新点一次目标末端触发热换；此状态下**启动机械臂会被拦住**，这是有意设计 |
| 「实际生效末端」一直显示"回读中..." | 参数服务没响应 | 检查 mission_executor 是否真的活着 |
| 柔触仍然会路边停车抓瓶 | C++ 没编译成功，跑的是旧的 | 回到第 1 步，确认 `colcon build` 真的成功了 |

---

## 7. 回滚

**只回滚代码（推荐）：**

```bash
cd /home/nvidia/kybot_ws
git revert --no-edit a2f8170 d1e9969 e3e71b3
colcon build --symlink-install --packages-select my_rviz_panel \
  --cmake-args -DDISTRO_ROS=humble
source install/setup.bash
```

**直接退回改动前的状态：**

```bash
git checkout d03fc1d -- src/my_rviz_panel tools/launcher.py
colcon build --symlink-install --packages-select my_rviz_panel \
  --cmake-args -DDISTRO_ROS=humble
```

**应急绕过（不改代码）：** 面板热换功能不用即可 —— 全停之后再切末端，
就是老流程。参数热改机制不会影响启动时传参的行为。

---

## 8. 已知未验证项 / 风险清单

按风险从高到低：

1. **C++ 从未编译过** —— 开发机是 x86_64 且没装 ROS。第 1 步可能报错。
2. **无任何实机验证** —— 全部改动只做过无头逻辑测试（108 项断言）和语法检查。
3. **Elite 机械臂驱动反复重启是否需要断电重连** —— 未知。不过"停机械臂 → 再启动"
   本来就是面板原有路径，热换只是复用它，所以风险不算新增。
4. **D435 反复起停的 USB 重枚举** —— `trash_mission.launch.py` 自带
   `initial_reset: 'true'` 会缓解，但二指↔柔触反复切时留意。
5. **热换后的第一次任务建议空跑** —— 先不放瓶子/不装工件，走一遍路点，
   确认动作白名单、停车距离、收臂服务都按新末端走。

---

## 9. 现场速查（最小路径）

时间紧的话至少做这些：

```bash
# 1. 编译
cd /home/nvidia/kybot_ws
colcon build --symlink-install --packages-select my_rviz_panel \
  --cmake-args -DDISTRO_ROS=humble && source install/setup.bash

# 2. 验参数（关键闸门，另开终端）
source /opt/ros/humble/setup.bash && source install/setup.bash
export ROS_DOMAIN_ID=42 RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
ros2 launch my_rviz_panel mission_executor.launch.py end_effector_mode:=twofinger
# 再开一个终端（同样 export）：
ros2 param set /mission_executor end_effector_mode softtouch   # 必须成功
ros2 param get /mission_executor end_effector_mode             # 必须变了
ros2 param set /mission_executor end_effector_mode banana      # 必须失败

# 3. 面板：选二指 → 启动定位+Nav → 等定位收敛
python3 tools/launcher.py

# 4. 记基准位姿 → 点柔触 → 等切换完成 → 确认位姿没跳
ros2 run tf2_ros tf2_echo map base_link

# 5. 物理换末端 → 确认"实际生效末端" → 启动机械臂
```
